sudo: false

language: cpp

compiler:
- gcc
- clang

before_script:
- mkdir build
- cd build
- cmake ..

script:
  - make

branches:
  only:
  - master

env:
  matrix:
    - BUILD_TYPE=Debug
    - BUILD_TYPE=Release

os:
- linux
- osx